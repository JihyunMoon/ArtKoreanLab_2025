###############################################
# Core AI provider (text analysis via Gemini) #
###############################################

# Which AI provider to use. Current options: gemini (default).
# If unset, defaults to "gemini" in code.
AI_PROVIDER=gemini

# Google AI (Gemini) API Key — required for sentiment/keywords analysis
# Use either GEMINI_API_KEY or GOOGLE_API_KEY (both are accepted by code)
# GEMINI_API_KEY=

# Optional: override the default Gemini text model (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash


##############################################
# Image generation provider (optional)       #
##############################################
# Required to enable ComfyUI path in the code
IMAGE_PROVIDER=comfyui

# ComfyUI server
COMFYUI_BASE_URL=http://127.0.0.1:8188

# Use your workflow; omit to default to ./default.json at repo root
COMFYUI_WORKFLOW_FILE=./default.json

# Optional overrides
COMFYUI_CKPT=v1-5-pruned-emaonly-fp16.safetensors
COMFYUI_WIDTH=512
COMFYUI_HEIGHT=512
COMFYUI_STEPS=20
COMFYUI_CFG=8
COMFYUI_SAMPLER=euler
COMFYUI_SCHEDULER=normal
# If you prefer to force a negative prompt override
COMFYUI_NEGATIVE=blurry, low quality, watermark, text, deformed, extra limbs, bad anatomy

# Timeouts (ms)
COMFYUI_TIMEOUT=180000
